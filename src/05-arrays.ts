const numbers = [1, 2, 3, 4];

const strs: string[] = [];
const strs2: Array<string> = [];

strs.push("sdf");

interface ICar2 {
  brand: string;
  wheels: number;
}

const cars: ICar2[] = [];

cars.push({ brand: "Audi", wheels: 2 });
